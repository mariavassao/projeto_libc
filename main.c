#include "libft.h"
#include <stdio.h>

#include <stdio.h>

int main() {
    char origem[10] = "Hello";
    char destino[10] = {0};
    ft_momvome(destino, origem, 6);  
    printf("Destino apos a copia: %s\n", destino);

    return 0;
}
